import { IconDefinition } from './icon-definition'
import { Category } from './category'

export const %GI_NAME%: IconDefinition = {
    categories: %CATEGORIES% as Array<Category>,
    name: '%NAME%',
    svgPath: '%PATH%',
    viewBox: '%VIEW_BOX%',
    width: '%WIDTH%'
};
